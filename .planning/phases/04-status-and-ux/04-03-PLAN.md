---
phase: 04-status-and-ux
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - tests/test_status.py
  - tests/test_output.py
autonomous: true
requirements: [STAT-01, STAT-02, STAT-03, STAT-04, STAT-05, STAT-06, STAT-07]

must_haves:
  truths:
    - "All STAT requirements have at least one automated test"
    - "Tests run without a real Telegram bot or Claude SDK"
    - "split_message edge cases are covered (code blocks, empty, exact boundary)"
  artifacts:
    - path: "tests/test_status.py"
      provides: "Tests for StatusUpdater lifecycle"
      contains: "test_"
    - path: "tests/test_output.py"
      provides: "Tests for split_message and TypingIndicator"
      contains: "test_"
  key_links:
    - from: "tests/test_status.py"
      to: "src/bot/status.py"
      via: "import and mock Bot"
      pattern: "from src.bot.status import"
    - from: "tests/test_output.py"
      to: "src/bot/output.py"
      via: "import and unit test"
      pattern: "from src.bot.output import"
---

<objective>
Write tests covering all STAT-01 through STAT-07 requirements using mocked Bot and SDK objects.

Purpose: Verify status, output, and error handling without real Telegram/Claude connections.
Output: Two test files covering the full status and UX pipeline.
</objective>

<execution_context>
@/Users/knyaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/knyaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-status-and-ux/04-CONTEXT.md
@.planning/phases/04-status-and-ux/04-01-SUMMARY.md
@.planning/phases/04-status-and-ux/04-02-SUMMARY.md

<interfaces>
From src/bot/status.py:
```python
class StatusUpdater:
    def __init__(self, bot: Bot, chat_id: int, thread_id: int): ...
    async def start_turn(self) -> None: ...
    def track_tool(self, tool_name: str) -> None: ...
    async def finalize(self, cost_usd: float | None, duration_ms: int, tool_count: int) -> None: ...
    async def stop(self) -> None: ...
```

From src/bot/output.py:
```python
def split_message(text: str, max_len: int = 4096) -> list[str]
class TypingIndicator:
    def __init__(self, bot: Bot, chat_id: int, thread_id: int): ...
    async def start(self) -> None: ...
    async def stop(self) -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tests for split_message and TypingIndicator</name>
  <files>tests/test_output.py</files>
  <read_first>src/bot/output.py, tests/conftest.py</read_first>
  <action>
Create `tests/test_output.py` with pytest tests:

**split_message tests (STAT-03, STAT-04):**

- `test_short_message_unchanged`: text < 4096 returns `[text]`
- `test_empty_message`: `""` returns `[""]`
- `test_exact_boundary`: text of exactly 4096 chars returns `[text]`
- `test_split_at_newline`: text of 5000 chars with a newline at position ~4000 splits there
- `test_split_preserves_code_block`: text with a code block (``` ... ```) before 4096 boundary — verify split happens at the code block boundary, not mid-block
- `test_split_multiple_chunks`: text of 10000 chars splits into 3+ parts, each <= 4096
- `test_hard_split_no_newline`: a single 5000-char string with no newlines splits at 4096

**TypingIndicator tests (STAT-05):**

- `test_typing_sends_action`: Create TypingIndicator with AsyncMock bot, start(), sleep briefly, stop(). Assert `bot.send_chat_action` was called with action="typing" and correct chat_id/thread_id.
- `test_typing_stop_cancels_task`: Start then stop. Assert internal task is cancelled/done.

Use `unittest.mock.AsyncMock` for the bot. Use `pytest.mark.asyncio` for async tests.
  </action>
  <verify>
    <automated>cd "/Users/knyaz/Telegram Multi-Thread Router" && python -m pytest tests/test_output.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - All split_message edge cases pass
    - TypingIndicator sends chat action with correct parameters
    - No real Telegram API calls made (all mocked)
  </acceptance_criteria>
  <done>test_output.py passes with coverage of STAT-03, STAT-04, STAT-05</done>
</task>

<task type="auto">
  <name>Task 2: Tests for StatusUpdater lifecycle</name>
  <files>tests/test_status.py</files>
  <read_first>src/bot/status.py, tests/conftest.py</read_first>
  <action>
Create `tests/test_status.py` with pytest tests:

**StatusUpdater tests (STAT-01, STAT-02, STAT-06, STAT-07):**

- `test_start_turn_sends_message` (STAT-01): Create StatusUpdater with AsyncMock bot (bot.send_message returns AsyncMock with message_id=42). Call start_turn(). Assert bot.send_message called with thread_id and "Working" in text. Stop the updater after.

- `test_track_tool_updates_state` (STAT-02): Create updater, call start_turn(), then track_tool("Read"). Assert `_current_tool == "Read"` and `_tool_count == 1`. Track another tool. Assert `_tool_count == 2`. Stop after.

- `test_finalize_edits_summary` (STAT-06): Create updater, start_turn() (mock send_message to return msg with message_id=42), then call finalize(cost_usd=0.0123, duration_ms=5000, tool_count=3). Assert bot.edit_message_text was called. Check that the edit text contains "$0.0123" and "5.0s" and "3 tools". Verify the refresh task was cancelled.

- `test_error_format` (STAT-07): This tests the runner integration. Use a simple assertion that the error format string pattern `"Error: {type}\n{message}"` is present in the runner source (via inspect.getsource). This confirms STAT-07 is wired.

- `test_stop_cancels_refresh` (STAT-01): start_turn(), then stop(). Assert the internal _update_task is done/cancelled.

Use `unittest.mock.AsyncMock` for bot. Mock `bot.send_message` to return an object with `.message_id = 42`. Use `pytest.mark.asyncio`.

Important: After each test that calls start_turn(), call stop() in cleanup to prevent dangling tasks. Use try/finally or pytest fixtures.
  </action>
  <verify>
    <automated>cd "/Users/knyaz/Telegram Multi-Thread Router" && python -m pytest tests/test_status.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - StatusUpdater lifecycle tested: start, track, finalize, stop
    - Mocked bot verifies correct Telegram API calls
    - No dangling asyncio tasks after tests complete
    - STAT-01, STAT-02, STAT-06, STAT-07 all have corresponding test assertions
  </acceptance_criteria>
  <done>test_status.py passes with coverage of STAT-01, STAT-02, STAT-06, STAT-07</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_output.py tests/test_status.py -x -v` — all tests pass
- `python -m pytest tests/ -x` — full test suite still passes (no regressions)
</verification>

<success_criteria>
All STAT-01 through STAT-07 requirements have automated test coverage. Tests run in under 10 seconds with no external dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/04-status-and-ux/04-03-SUMMARY.md`
</output>
