---
phase: 05-voice-and-file-io
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - tests/test_voice_and_files.py
autonomous: true
requirements:
  - INPT-02
  - INPT-03
  - INPT-04
  - FILE-01
  - FILE-02
  - FILE-03
  - FILE-04

must_haves:
  truths:
    - "Voice transcription module works with semaphore guard"
    - "MCP tools factory creates working tools with correct behavior"
    - "Session router voice handler transcribes and enqueues"
    - "Session router photo/document handlers download and enqueue"
    - "send_file rejects files over 50MB"
  artifacts:
    - path: "tests/test_voice_and_files.py"
      provides: "Test suite for Phase 5 voice, file input, and MCP tools"
      min_lines: 100
  key_links:
    - from: "tests/test_voice_and_files.py"
      to: "src/sessions/voice.py"
      via: "mock faster-whisper, test transcribe_voice"
      pattern: "transcribe_voice"
    - from: "tests/test_voice_and_files.py"
      to: "src/sessions/mcp_tools.py"
      via: "mock Bot, test tool functions"
      pattern: "create_telegram_mcp_server"
---

<objective>
Create the test suite for all Phase 5 functionality: voice transcription, photo/document handlers, and MCP output tools.

Purpose: Verify INPT-02, INPT-03, INPT-04, FILE-01 through FILE-04 with automated tests.
Output: `tests/test_voice_and_files.py` covering all 7 requirements.
</objective>

<execution_context>
@/Users/knyaz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/knyaz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-voice-and-file-io/05-01-SUMMARY.md
@.planning/phases/05-voice-and-file-io/05-02-SUMMARY.md
@src/sessions/voice.py
@src/sessions/mcp_tools.py
@src/bot/routers/session.py
@tests/conftest.py
@tests/test_permissions.py
</context>

<interfaces>
<!-- From Plan 01 outputs -->

From src/sessions/voice.py:
```python
_model: WhisperModel | None  # module-level, lazy-loaded
_semaphore: asyncio.Semaphore  # Semaphore(1)
async def transcribe_voice(ogg_path: str) -> str
```

From src/sessions/mcp_tools.py:
```python
def create_telegram_mcp_server(bot: Bot, chat_id: int, thread_id: int) -> MCPServer
# Tools: reply(text), send_file(path), react(emoji, message_id), edit_message(text, message_id)
```

<!-- From existing test patterns -->

From tests/conftest.py:
```python
# Sets default env vars for config imports during test collection
# Pattern: monkeypatch or os.environ in conftest
```

From tests/test_permissions.py (AsyncMock bot pattern):
```python
bot = AsyncMock(spec=Bot)
bot.send_message = AsyncMock()
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Voice transcription and MCP tools unit tests</name>
  <files>tests/test_voice_and_files.py</files>
  <read_first>
    - tests/conftest.py (env var setup pattern)
    - tests/test_permissions.py (AsyncMock bot pattern)
    - src/sessions/voice.py (implementation to test)
    - src/sessions/mcp_tools.py (implementation to test)
    - src/bot/routers/session.py (handlers to test)
  </read_first>
  <action>
Create `tests/test_voice_and_files.py` with these test groups:

**Imports:**
```python
import asyncio
import os
from pathlib import Path
from unittest.mock import AsyncMock, MagicMock, patch, PropertyMock
import pytest
from aiogram import Bot
from aiogram.types import ReactionTypeEmoji
```

**Group 1: Voice transcription (INPT-02)**

`test_transcribe_voice_returns_text`:
- Mock `faster_whisper.WhisperModel` at module level via `patch("src.sessions.voice.WhisperModel")`
- Set the mock model's `.transcribe()` to return `(mock_segments, mock_info)` where mock_segments is a list of objects with `.text` attributes like `[" Hello ", " world "]`
- Reset module state: `voice._model = None` before test
- Call `await transcribe_voice("/tmp/test.ogg")`
- Assert result == "Hello world"
- Assert WhisperModel was called with `("medium", compute_type="int8", device="cpu")`

`test_transcribe_voice_semaphore_prevents_concurrent`:
- Mock WhisperModel as above but make transcribe block with an asyncio.Event
- Start two transcribe_voice tasks concurrently
- Assert only one is actually running (semaphore blocks the second)
- Release the event, both complete
- Reset `voice._model = None` and `voice._semaphore = asyncio.Semaphore(1)` in teardown

`test_transcribe_voice_lazy_loads_model`:
- Mock WhisperModel
- Reset `voice._model = None`
- Call transcribe_voice once — WhisperModel instantiated
- Call transcribe_voice again — WhisperModel NOT instantiated again (still 1 call)

**Group 2: MCP tools (FILE-01 through FILE-04)**

`test_reply_tool_sends_message`:
- `bot = AsyncMock(spec=Bot)`
- `server = create_telegram_mcp_server(bot, chat_id=123, thread_id=456)`
- Extract the reply tool function from server (examine server's tool registry)
- Call it with text="Hello"
- Assert `bot.send_message.called_with(chat_id=123, message_thread_id=456, text="Hello")`

`test_send_file_tool_sends_document`:
- Create a temp file with some content (< 50MB)
- `bot = AsyncMock(spec=Bot)`
- `server = create_telegram_mcp_server(bot, chat_id=123, thread_id=456)`
- Call send_file tool with the temp file path
- Assert `bot.send_document` was called
- Clean up temp file

`test_send_file_tool_rejects_oversized`:
- Create a mock scenario: patch `Path.stat` to return a size > 50MB
- Call send_file tool
- Assert result contains "50MB" or "limit"
- Assert `bot.send_document` was NOT called

`test_send_file_tool_rejects_missing`:
- Call send_file tool with nonexistent path "/tmp/nonexistent_file_xyz"
- Assert result contains "Error" or "not found" or similar
- Assert `bot.send_document` was NOT called

`test_react_tool_adds_reaction`:
- `bot = AsyncMock(spec=Bot)`
- `server = create_telegram_mcp_server(bot, chat_id=123, thread_id=456)`
- Call react tool with emoji="👍", message_id=789
- Assert `bot.set_message_reaction` was called

`test_edit_message_tool_edits`:
- `bot = AsyncMock(spec=Bot)`
- `server = create_telegram_mcp_server(bot, chat_id=123, thread_id=456)`
- Call edit_message tool with text="Updated", message_id=789
- Assert `bot.edit_message_text` was called

**Group 3: Runner MCP wiring**

`test_runner_creates_mcp_server`:
- Use `inspect.getsource` on `SessionRunner._run` to confirm it contains "create_telegram_mcp_server" and 'mcp_servers' — same pattern as Phase 4's test_error_format test
- This avoids needing to actually run the SDK

**Note on tool extraction:** The exact method to get tool functions from the MCP server depends on the create_sdk_mcp_server API. If tools are registered as methods on the server object, access them directly. If the API uses a different pattern, adapt accordingly. The test should verify the tool's effect (bot method called) not the internal registration mechanism. If direct tool extraction is not straightforward, test by calling the inner functions directly if they are accessible, or use inspect.getsource to verify wiring.
  </action>
  <verify>
    <automated>cd "/Users/knyaz/Telegram Multi-Thread Router" && python -m pytest tests/test_voice_and_files.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All tests pass
    - INPT-02 covered: voice transcription returns joined text, semaphore works, lazy loading works
    - FILE-01 covered: reply tool calls bot.send_message
    - FILE-02 covered: send_file tool calls bot.send_document, rejects >50MB and missing files
    - FILE-03 covered: react tool calls bot.set_message_reaction
    - FILE-04 covered: edit_message tool calls bot.edit_message_text
    - Runner wiring verified (MCP server creation confirmed in source)
  </acceptance_criteria>
  <done>Test suite passes, covering all 7 Phase 5 requirements with automated assertions</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_voice_and_files.py -x -v` — all tests pass
- `python -m pytest tests/ -x` — no regressions in existing tests
</verification>

<success_criteria>
- 8+ tests covering INPT-02, INPT-03, INPT-04, FILE-01, FILE-02, FILE-03, FILE-04
- All pass without needing real Telegram API or faster-whisper model
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-voice-and-file-io/05-03-SUMMARY.md`
</output>
